# CPU-only PyTorch build — the default PyPI wheel drags in ~15 nvidia-cuda-*
# packages (multi-GB) that a GPU-less Render web service never uses.
--extra-index-url https://download.pytorch.org/whl/cpu

streamlit==1.55.0
python-dotenv==1.2.2
markdown==3.10.2

langchain==0.1.17
langchain-core==0.1.53
langchain-community==0.0.36
langchain-openai==0.1.5
langchain-text-splitters==0.0.2

chromadb==1.5.5
pymupdf==1.28.0
rank-bm25==0.2.2
sentence-transformers==5.3.0
torch==2.10.0+cpu
